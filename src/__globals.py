# A dictionary that contains the list of handlers per window,
# key is based on `sublime.Window.id()`
FILE_WATCHER_WINDOWS = {}

# A dictionary that contains the "handler" per directory,
# key is based on `Watcher.file_watcher._last_controller_id`
FILE_WATCHER_HANDLERS = {}
